instance test_patients of heli is 

  object Patient1 of Patient is
    id := 1;
    goal := Goal(
      typ := enum control_weight(
      );
      val := 75.0;
    );
    t_registered := +0.0;
    act := Activity(
      weight := 90.0;
      steps := 0;
      fruits := 0;
    );
    t_last_activity := +0.0;
    m := 1.0;
    ns := 0.0;
    v := 0.0;
    adh := 0.0;
    prob := 0.0;
    coachId := 0;
  end
  object Patient2 of Patient is
    id := 2;
    goal := Goal(
      typ := enum walk_steps_aweek(
      );
      val := 10000.0;
    );
    t_registered := +0.0;
    act := Activity(
      weight := 0.0;
      steps := 1000;
      fruits := 0;
    );
    t_last_activity := +0.0;
    m := 1.0;
    ns := 0.0;
    v := 0.0;
    adh := 0.0;
    prob := 0.0;
    coachId := 0;
  end
  object Patient3 of Patient is
    id := 3;
    goal := Goal(
      typ := enum eat_fruits_aweek(
      );
      val := 5.0;
    );
    t_registered := +0.0;
    act := Activity(
      weight := 0.0;
      steps := 0;
      fruits := 2;
    );
    t_last_activity := +0.0;
    m := 1.0;
    ns := 0.0;
    v := 0.0;
    adh := 0.0;
    prob := 0.0;
    coachId := 0;
  end
  object Coach1 of Coach is
    PQ := {
    };
  end
  object HeliWeb1 of HeliWeb is
    PQ := <
    >;
    CQ := <
    >;
  end


  omega := +0.0;
  deadlines := {};
end
